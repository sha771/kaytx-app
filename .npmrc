# pnpm configuration to fix package resolution issues
# Disable strict exports to allow fallback to file-based resolution
resolve-pnp-from-unstable=false
# Use node-modules layout for better compatibility
node-linker=hoisted
# Enable auto-install-peers
auto-install-peers=true